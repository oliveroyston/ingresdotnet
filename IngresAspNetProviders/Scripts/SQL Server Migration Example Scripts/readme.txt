These scripts demonstrate how to migrate data from SQL Server to Ingres.

These scripts will need altering depending on your environment. The export from SQL Server is done
via BCP and the import to Ingres is done via the Ingres 'COPY TABLE' SQL command.